
1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Modify the `GEMINI_BASE_URL` in  [.env.local](.env.local) and  [vite.config.ts](vite.config.ts) and [geminiService.ts](geminiService.ts)
4. Run the app:
   `npm run dev`
